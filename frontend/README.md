# LuckyReads frontend

Vite + React + TypeScript SPA. Routes are defined in `src/app/routes.ts`; `App.tsx` mounts the shared `RouterProvider`.

From the repo root, see the main [README](../README.md) for `npm run dev` / Docker. In this directory: `npm ci` then `npm run dev` (default dev server URL is printed in the terminal).
